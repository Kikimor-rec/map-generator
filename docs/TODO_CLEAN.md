# TODO (clean summary)

This file tracks high-signal action items while the legacy dev plan remains corrupted.

- **Corridor creation/routing still needs tuning**: keep seeds that produce clutter; apply Corridor Routing Engine v2 phases as planned.
- **Docs consistency**: routing/coalesce/quality docs updated; re-check whenever behavior changes.
- **Segment selection**: editor still selects whole corridor; enable per-segment selection/highlight.
- **Locked waypoints & incremental reroute**: not implemented (Phase 3).
- **Long runs indicator**: panel now shows >1s indicator; keep UX review after further changes.
